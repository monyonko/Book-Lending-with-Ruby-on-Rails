
<div class="enclose_form">
    <% if alert = flash[:alert] %>
    <%= alert %>
    <% end %>
    <%= form_with model: @user, url: registrations_url, class: "contents" do |form| %>

    <h2 class="form_heading">Sign Up</h2>
    <div className="form_field">
        <%= form.label :email%>
        <%= form.email_field :email_address, required: true, autofocus: true, value: params[:email_address]%>
    </div>
    <div className="form_field">
        <%= form.label :password%>
        <%= form.password_field :password, required: true, placeholder: "Enter your password", maxlength: 72%>
    </div>
    <div className="form_field">
        <%= form.label :password_confirmation%>
        <%= form.password_field :password, required: true, placeholder: "Confirm your password", maxlength: 72%>
    </div>

</div>



Sign up

 

   
   

   
     
       <%= form.submit "Sign up", class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block cursor-pointer" %>

   
 <% end %>
